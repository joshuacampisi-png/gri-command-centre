# Vitals backend config. Copy to .env (or set in Railway/hosting dashboard).

# Port the Express server listens on (Railway sets this automatically).
PORT=8787

# Set to "production" in deployment so the auth cookie is marked Secure (HTTPS-only).
NODE_ENV=production
