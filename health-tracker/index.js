import { registerRootComponent } from 'expo';
import App from './App';

// Entry point. registerRootComponent calls AppRegistry.registerComponent
// and ensures the environment is set up correctly for Expo Go and builds.
registerRootComponent(App);
